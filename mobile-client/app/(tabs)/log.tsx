/* Calorie log tab — placeholder until the log screen is built. */

import { View, Text } from 'react-native'

export default function LogScreen() {
  return (
    <View className="flex-1 justify-center items-center bg-white">
      <Text className="text-base text-gray-500">Log coming soon</Text>
    </View>
  )
}
