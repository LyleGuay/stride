/* Settings tab — placeholder until the settings screen is built. */

import { View, Text } from 'react-native'

export default function SettingsScreen() {
  return (
    <View className="flex-1 justify-center items-center bg-white">
      <Text className="text-base text-gray-500">Settings coming soon</Text>
    </View>
  )
}
