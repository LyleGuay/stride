# Future Ideas

## Auth

- **Neon Auth** — Managed auth built on Better Auth, stores users/sessions in Postgres. Supports OAuth (Google out of the box), session management, SQL-queryable user data. Worth considering if Stride becomes multi-user. https://neon.com/docs/auth/overview
