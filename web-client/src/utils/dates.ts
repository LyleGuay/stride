// Date utilities live in packages/shared — re-exported here so existing import paths still work.
export { todayString, getMondayOf, shiftWeek, formatWeekRange, dayLabel, dayNumber } from '@stride/shared'
