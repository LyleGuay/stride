/* Re-exports from @stride/shared — single source of truth for calorie log constants. */
export { ITEM_TYPES, FOOD_UNITS, EXERCISE_UNITS, UNIT_LABELS, MEAL_PLAN_MEAL_TYPES } from '@stride/shared'
export type { MealPlanMealType } from '@stride/shared'
